<!DOCTYPE html>
<html lang="ko">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>사용자 프로필</title>
        <link rel="stylesheet" href="/user/user-profile.css" />
        <link
            rel="stylesheet"
            href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
        />
    </head>
    <body>
        <script>
            (function() {
                const savedTheme = localStorage.getItem('theme') || 'light';
                document.documentElement.setAttribute('data-theme', savedTheme);
            })();
        </script>
        <div class="container">
            <div class="user-profile-header">
                <h1 class="title" id="page-title">사용자 프로필</h1>
                <a href="/" class="back-button">
                    <span class="material-symbols-outlined">arrow_back</span>
                    홈으로 돌아가기
                </a>
            </div>

            <div id="loading" class="loading">프로필을 불러오는 중...</div>

            <div
                id="error-container"
                class="error-container"
                style="display: none"
            >
                <div class="error-message" id="error-message">
                    사용자를 찾을 수 없습니다.
                </div>
            </div>

            <div id="profile-container" style="display: none">
                <div class="profile-section">
                    <div class="profile-image-container">
                        <div class="profile-image" id="profile-image">
                            <span class="material-symbols-outlined"
                                >person</span
                            >
                        </div>
                    </div>

                    <div class="profile-info">
                        <div class="info-item">
                            <label>아이디</label>
                            <div class="info-value" id="userid"></div>
                        </div>
                        <div class="info-item">
                            <label>닉네임</label>
                            <div class="info-value" id="username"></div>
                        </div>
                        <div class="info-item">
                            <label>소개</label>
                            <div class="info-value" id="description">
                                소개가 없습니다.
                            </div>
                        </div>
                        <div class="info-item">
                            <label>가입일</label>
                            <div class="info-value" id="created-at"></div>
                        </div>
                        <div class="info-item">
                            <label>UID</label>
                            <div class="info-value" id="uid"></div>
                        </div>
                    </div>
                </div>

                <div class="action-buttons">
                    <button class="guestbook-button" id="guestbook-button">
                        <span class="material-symbols-outlined">comment</span>
                        방명록 쓰기
                    </button>

                    <button
                        class="article-list-button"
                        id="article-list-button"
                    >
                        <span class="material-symbols-outlined">article</span>
                        활동 목록 보기
                    </button>

                    <button class="message-button" id="message-button">
                        <span class="material-symbols-outlined">message</span>
                        메시지 보내기
                    </button>
                </div>
            </div>
        </div>

        <div class="notice-container"></div>

        <script type="module" src="/user/user-profile.mjs"></script>
    </body>
</html>
